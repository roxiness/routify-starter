{
  "scripts": {
    "build": "npm run build:app && npm run build:netlify",
    "build:app": "cd ../.. && npm i && npm run build",
    "build:netlify": "node build",
    "deploy": "npm run build:netlify && npx netlify deploy"
  },
  "dependencies": {
    "fs-extra": "^8.1.0"
  }
}