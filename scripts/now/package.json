{
  "scripts": {
    "build": "node build",
    "build:app": "cd ../.. && npm i && npm run build",
    "deploy": "npm run build && npx now"
  },
  "dependencies": {
    "@sveltech/ssr": "^0.0.9",
    "fs-extra": "^8.1.0"
  }
}